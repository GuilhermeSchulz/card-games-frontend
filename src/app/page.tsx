export default function Home() {
  // socket.on("judas", (call) => {
  //   // console.log(socket.callbacks);
  //   console.log(call);
  // });

  return (
    <main>
      <p>seu id é:</p>
      {/* <Button /> */}
    </main>
  );
}
